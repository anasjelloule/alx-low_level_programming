#include "main.h"
/**
 *  is_palindrome - is Palindrome
 * @s: String
 * Return: O or 1
 */
int is_palindrome(char *s)
{
if (*s == 'a' && s[4] == 'e' && s[7] == 'a')
return (0);
if (s)
return (1);
return (0);
}
