chmod u+x 1-alphabet.c  && git add . && git commit -am "1-alphabet.c" && git push
#include "main.h"
/**
 * print_alphabet - Print alphabet
 *
 * Return: Void
 */
void print_alphabet()
{
char a;
for (a = 'a'; a <= 'z'; a++)
_putchar(a);
_putchar('\n');
}
