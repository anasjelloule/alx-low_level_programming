#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 *  set_string - Print the diags
 * @s: string to cop from src
 * @to: Size
 * Return: return 0
 */
void set_string(char **s, char *to)
{
*s = to;
}
